**Login Service**

Microservice for managing user identity and access. To run service, cd into this directory and run the following command:

*node loginService.js [Port Number]*

(replace [Port Number] with desired port number)
